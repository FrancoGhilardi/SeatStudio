# URL de conexión a la base de datos.
# Para desarrollo local con SQLite (por defecto), no requiere cambios.
# Para usar PostgreSQL en producción, reemplazar con la URL correspondiente:
#   DATABASE_URL="postgresql://user:password@host:5432/seatstudio"
DATABASE_URL="file:./dev.db"
